{% comment %}theme-check-disable UndefinedObject{% endcomment %}
<!-- StyloDyna Why Choose Section -->
{{ 'section-stylodyna-why-choose.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.6 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.6 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top | times: 0.8 | round: 0 }}px;
      padding-bottom: {{ section.settings.padding_bottom | times: 0.8 | round: 0 }}px;
    }
  }

  @media screen and (min-width: 990px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

<div class="stylodyna-why-choose section-{{ section.id }}-padding{% if section.settings.enable_animation %} stylodyna-why-choose--animated{% endif %}">
  <div class="stylodyna-why-choose__inner">
    
    <!-- Section Header -->
    <div class="stylodyna-why-choose__header">
      {%- if section.settings.eyebrow != blank -%}
        <p class="stylodyna-why-choose__eyebrow">{{ section.settings.eyebrow | escape }}</p>
      {%- endif -%}

      {%- if section.settings.heading != blank -%}
        <h2 class="stylodyna-why-choose__heading">
          {{ section.settings.heading | escape | newline_to_br }}
        </h2>
      {%- endif -%}

      {%- if section.settings.supporting_text != blank -%}
        <div class="stylodyna-why-choose__description rte">
          {{ section.settings.supporting_text }}
        </div>
      {%- endif -%}
    </div>

    <!-- Trust Cards Grid -->
    <div class="stylodyna-why-choose__grid">
      {%- for block in section.blocks -%}
        {%- if block.type == 'trust_card' -%}
          <div class="stylodyna-why-choose__card" {{ block.shopify_attributes }}>
            
            <!-- Number Badge -->
            {%- if block.settings.number != blank -%}
              <span class="stylodyna-why-choose__number">{{ block.settings.number | escape }}</span>
            {%- else -%}
              <span class="stylodyna-why-choose__number">0{{ forloop.index }}</span>
            {%- endif -%}

            <!-- Soft Ivory Circular Icon Container (88px) -->
            <div class="stylodyna-why-choose__icon-container">
              <span class="stylodyna-why-choose__icon">
                {%- case block.settings.icon -%}
                  {%- when 'shield' -%}
                    <svg viewBox="0 0 24 24" aria-hidden="true" xmlns="http://www.w3.org/2000/svg">
                      <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
                    </svg>
                  {%- when 'truck' -%}
                    <svg viewBox="0 0 24 24" aria-hidden="true" xmlns="http://www.w3.org/2000/svg">
                      <rect x="1" y="3" width="15" height="13" rx="1"/>
                      <polygon points="16 8 20 8 23 11 23 16 16 16 16 8"/>
                      <circle cx="5.5" cy="18.5" r="2.5"/>
                      <circle cx="18.5" cy="18.5" r="2.5"/>
                    </svg>
                  {%- when 'heart' -%}
                    <svg viewBox="0 0 24 24" aria-hidden="true" xmlns="http://www.w3.org/2000/svg">
                      <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l8.72-8.72 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
                    </svg>
                  {%- else -%}
                    <!-- Leaf / Chair Line Icon -->
                    <svg viewBox="0 0 24 24" aria-hidden="true" xmlns="http://www.w3.org/2000/svg">
                      <path d="M11 20A9 9 0 0 1 2 11 9 9 0 0 1 11 2c5 0 9 4 9 9"/>
                      <path d="M11 2a9 9 0 0 1 9 9c0 5-4 9-9 9"/>
                      <path d="M2 21c0-3 1.85-5.36 5.08-6C9.5 14.52 12 13 13 12"/>
                    </svg>
                {%- endcase -%}
              </span>
            </div>

            <!-- Card Content -->
            {%- if block.settings.title != blank -%}
              <h3 class="stylodyna-why-choose__card-title">{{ block.settings.title | escape }}</h3>
            {%- endif -%}

            {%- if block.settings.description != blank -%}
              <p class="stylodyna-why-choose__card-body">{{ block.settings.description | escape }}</p>
            {%- endif -%}

          </div>
        {%- endif -%}
      {%- endfor -%}
    </div>

  </div>
</div>

{% schema %}
{
  "name": "Why Choose StyloDyna",
  "tag": "section",
  "class": "section-stylodyna-why-choose",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow Label",
      "default": "WHY CHOOSE STYLODYNA"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Thoughtfully Designed.\nProfessionally Delivered."
    },
    {
      "type": "richtext",
      "id": "supporting_text",
      "label": "Supporting Text",
      "default": "<p>Every collection is carefully selected to combine timeless aesthetics, dependable quality, and an effortless shopping experience—helping you create a home you'll enjoy for years to come.</p>"
    },
    {
      "type": "checkbox",
      "id": "enable_animation",
      "label": "Enable Entrance Animation",
      "default": true
    },
    {
      "type": "header",
      "content": "Section Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 160,
      "step": 8,
      "unit": "px",
      "label": "Top Padding",
      "default": 120
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 160,
      "step": 8,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 120
    }
  ],
  "blocks": [
    {
      "type": "trust_card",
      "name": "Trust Feature",
      "limit": 4,
      "settings": [
        {
          "type": "text",
          "id": "number",
          "label": "Number Badge",
          "default": "01"
        },
        {
          "type": "select",
          "id": "icon",
          "label": "SVG Line Icon",
          "options": [
            { "value": "leaf", "label": "Leaf / Design" },
            { "value": "shield", "label": "Shield / Craftsmanship" },
            { "value": "truck", "label": "Truck / Delivery" },
            { "value": "heart", "label": "Heart / Support" }
          ],
          "default": "leaf"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Feature Title"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description",
          "default": "Feature description text explaining the benefit to the customer."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Why Choose StyloDyna",
      "blocks": [
        {
          "type": "trust_card",
          "settings": {
            "number": "01",
            "icon": "leaf",
            "title": "Timeless Design",
            "description": "Modern British and Scandinavian-inspired furniture curated to remain elegant beyond changing trends."
          }
        },
        {
          "type": "trust_card",
          "settings": {
            "number": "02",
            "icon": "shield",
            "title": "Premium Craftsmanship",
            "description": "Quality materials, refined finishes, and products selected for everyday durability and lasting value."
          }
        },
        {
          "type": "trust_card",
          "settings": {
            "number": "03",
            "icon": "truck",
            "title": "Reliable Delivery",
            "description": "Carefully packaged and delivered across Pakistan with attention to safety and customer satisfaction."
          }
        },
        {
          "type": "trust_card",
          "settings": {
            "number": "04",
            "icon": "heart",
            "title": "Dedicated Support",
            "description": "Friendly assistance before and after your purchase to make your shopping experience simple and enjoyable."
          }
        }
      ]
    }
  ]
}
{% endschema %}
